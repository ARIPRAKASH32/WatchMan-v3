# Watch Man — Network Traffic Analyzer
# Requirements file — install with: pip install -r requirements.txt

# Web framework
flask>=3.0.0,<4.0.0
flask-cors>=4.0.0,<5.0.0

# Packet capture (optional — only needed for live capture mode, requires root)
# Install separately if needed: pip install scapy
scapy>=2.5.0

# System metrics (optional — used for future interface enumeration)
psutil>=5.9.0

# Standard library modules used (no install needed):
#   threading, socket, collections, time, csv, io, logging, atexit, pathlib

# Reporting
fpdf2>=2.7.5

# AI / ML & IDRS dependencies
scikit-learn>=1.3.0
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0
